__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.resint/
*.sarif
.DS_Store
sweeps/
.cache/

# Secrets never belong in the repo. resint reads API keys from the
# environment only -- see model/openai_compat.py -- so nothing here should
# ever hold one. These lines are a safety net, not the mechanism.
.env
.env.*
*.key
secrets.*
