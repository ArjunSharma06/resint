% Positive fixture for stats/significance-unsupported.
%
% The paper asserts reliable differences repeatedly and reports no test
% statistic, no p-value, no interval, and no variance anywhere. The rule is
% document-level on purpose: a paper that runs its tests and describes them
% properly must never land here, even if one sentence reads loosely.
\documentclass{article}
\begin{document}

\section{Results}

Our method is significantly better than the baseline across every
configuration we examined.

The gain on the held-out split was a significant improvement, and the
ablation confirms that removing the adapter significantly reduces quality.

We therefore conclude that the approach reliably outperforms prior work.

\end{document}
